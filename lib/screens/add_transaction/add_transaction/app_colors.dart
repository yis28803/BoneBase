import 'package:flutter/material.dart';

/// Màu sắc dùng chung trong toàn bộ Add Transaction flow.
class AppColors {
  const AppColors._();

  static const background = Color(0xFF1C1C1E);
  static const card = Color(0xFF2C2C2E);
}